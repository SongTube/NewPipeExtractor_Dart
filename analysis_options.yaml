include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    # The public API of this package predates the effective-dart file naming
    # convention (camelCase filenames); renaming would be a breaking change.
    file_names: ignore
